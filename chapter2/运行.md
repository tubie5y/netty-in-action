1. `mvn install -pl utils`
2. `mvn clean package`
3. 运行Echo服务器和客户端
    - 要运行这些应用程序组件，可以直接使用Java命令。但是在POM文件中，已经为你配置好了`exec-maven-plugin`来做这个（参见附录以获取详细信息）。
      并排打开两个控制台窗口，一个进到`chapter2\Server`目录中，另外一个进到`chapter2\Client`目录中。
      在服务器的控制台中执行这个命令：`mvn exec:java`
